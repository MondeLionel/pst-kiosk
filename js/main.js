// initialize swiper


// images only

const mainSwiper = new Swiper('.swiper-container',{
	autoplay: {
       delay: 5000,
     }
})







// Only used for "manual" slide changes
// mainSwiper.on("slideNextTransitionStart", function(){
//     const video = document.querySelector("video");
//     if(video.closest(".swiper-slide-active") !== null){
//         video.play()
//     }else{
//         video.pause()
//     }
// })
